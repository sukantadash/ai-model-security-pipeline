apiVersion: batch/v1
kind: Job
metadata:
  name: guidellm-benchmark
  namespace: demo-llm
spec:
  backoffLimit: 0
  template:
    spec:
      restartPolicy: Never
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
      - name: guidellm
        image: ghcr.io/vllm-project/guidellm:latest
        env:
          # Replace REPLACE_WITH_CLUSTER_APPS_DOMAIN (see script.sh Phase 12 / optional GuideLLM).
          #   APPS_DOMAIN=$(oc get ingresses.config/cluster -o jsonpath='{.spec.domain}')
          # APPS_DOMAIN already includes the apps. prefix.
          - name: GUIDELLM_TARGET
            value: "https://inference-gateway.REPLACE_WITH_CLUSTER_APPS_DOMAIN/model-test/qwen3-8b-fp8"
          - name: GUIDELLM_PROFILE
            value: throughput
          - name: GUIDELLM_RATE
            value: "20"  # max concurrent streams (required by GuideLLM >=0.5 for throughput profile)
          - name: GUIDELLM_MAX_SECONDS
            value: "60"
          # 20 distinct 2048-token prefixes (simulating 20 different large documents/chats)
          - name: GUIDELLM_DATA
            value: "prefix_tokens=2048,prefix_count=20,prompt_tokens=100,output_tokens=50"
          - name: HF_HOME
            value: /tmp/hf-cache
          - name: TRANSFORMERS_CACHE
            value: /tmp/hf-cache
          - name: XDG_CACHE_HOME
            value: /tmp
        volumeMounts:
          - name: cache
            mountPath: /tmp/hf-cache
          - name: results
            mountPath: /results
      volumes:
        - name: cache
          emptyDir: {}
        - name: results
          emptyDir: {}