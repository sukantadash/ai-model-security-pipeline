# Update hostname before applying overlay 13-gateway.
# model-prod is the verified-model serving namespace (HLD §4.3); do not add hf:// demo namespaces here.
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  labels:
    istio.io/rev: openshift-gateway
  name: openshift-ai-inference
  namespace: openshift-ingress
spec:
  gatewayClassName: openshift-ai-inference
  listeners:
    - allowedRoutes:
        namespaces:
          from: Selector
          selector:
            matchExpressions:
              - key: kubernetes.io/metadata.name
                operator: In
                values:
                  - model-prod
      hostname: inference-gateway.apps.cluster-bvbpt.bvbpt.sandbox5358.opentlc.com
      name: https
      port: 443
      protocol: HTTPS
      tls:
        certificateRefs:
          - group: ''
            kind: Secret
            name: openshift-ai-inference-tls
        mode: Terminate
