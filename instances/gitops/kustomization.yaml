# Single apply entrypoint for App-of-Apps root Application.
# Child Applications live under apps/ and are synced by Argo CD.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - application-root.yaml
