apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: minio-system

resources:
  - namespace.yaml
  - secret.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - bucket-init-job.yaml
