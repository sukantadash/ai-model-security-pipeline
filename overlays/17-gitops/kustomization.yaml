# Phase 16: App-of-Apps root (ai-model-security-platform) → instances/gitops/apps.
# Prerequisites: OpenShift GitOps Ready; prefer ./gitops-scripts.sh for post-sync.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../instances/gitops
