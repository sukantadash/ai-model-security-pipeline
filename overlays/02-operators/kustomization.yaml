# Phase 1: Platform operators — security pipeline + RHOAI inference stack.
# Prerequisites: cluster-admin, OLM catalog (redhat-operators), cert-manager for gateway (Phase 12).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../operators/openshift-pipelines
  - ../../operators/openshift-gitops
  - ../../operators/sandboxed-containers
  - ../../operators/servicemesh
  - ../../operators/connectivity-link
  - ../../operators/leaderworkerset
  - ../../operators/rhoai
