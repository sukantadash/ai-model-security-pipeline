# Phase 15: Verified LLMInferenceService + test-user RBAC in model-test.
# Prerequisites: Overlays 11–15; see instances/model-test/README.md.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../instances/model-test
