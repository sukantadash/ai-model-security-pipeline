# Phase 10: DataScienceCluster and DSCInitialization.
# Prerequisites: RHOAI operator CSV Succeeded (Phase 1).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../instances/rhoai
