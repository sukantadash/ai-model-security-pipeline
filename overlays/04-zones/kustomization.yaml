# Phase 3: Three security zones — namespaces, NetworkPolicies, pipeline RBAC.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../instances/model-ingress
  - ../../instances/model-eval
  - ../../instances/model-prod
  - ../../instances/pipeline-rbac
